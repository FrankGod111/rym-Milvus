# v0.5.0 优化路线图

## 版本目标

`v0.5.0` 的核心目标不是继续堆功能，而是把当前 ERP 文档管理 + Dify 智能问答 MVP，提升到“权限边界更清晰、调用链更可靠、数据层可扩展、运维更可控”的下一阶段版本。

本版本重点解决 4 类问题：

- 认证和权限仍偏演示态，接口存在被伪造身份调用的风险
- AI 问答链路存在“先检索后过滤”问题，企业知识访问边界不够硬
- 本地 `JSON + 文件` 存储适合单机演示，不适合持续扩展
- 上传、解析、同步、索引仍是同步流程，缺乏任务化和可观测性

## 版本范围

### P0 必做

- 统一认证鉴权
- 强化文档权限与管理权限校验
- 修正 AI / Dify 权限过滤链路
- 引入异步任务机制处理上传解析和同步
- 为后续数据库迁移完成存储抽象
- 增加基础测试和日志观测

### P1 建议做

- 列表分页、排序、筛选增强
- 审计日志增强
- 设置项白名单和类型校验
- 批量操作结果回执增强

### 本版本不强制纳入

- OCR 正式接入
- 在线文档编辑器
- 企业 SSO / LDAP / OAuth
- 水印、病毒扫描、全文审批流
- 多知识库智能路由

## 里程碑

### 里程碑 1：认证与权限收口

目标：

- 所有 ERP 接口不再信任前端传入的 `user_id` 或 `user`
- 管理员、编辑、只读角色的能力边界明确化
- 敏感接口必须经过服务端鉴权

改造项：

- 新增认证中间件或依赖注入层，从 token 中解析当前用户
- 登录接口返回真正可校验的 token，至少支持签名和过期时间
- 为以下接口增加角色校验：
  - 文档删除
  - 批量归档
  - 权限修改
  - 系统设置修改
  - Dify 配置修改
  - Dify 同步与状态刷新
- 服务端统一提供 `current_user` 上下文，替代查询参数传用户身份

验收标准：

- 前端删除 `user_id` 显式透传后，文档列表、详情、AI 问答仍可正常工作
- 非管理员无法修改系统设置和其他人的权限配置
- 未登录请求访问受保护接口时返回 `401`
- 已登录但权限不足时返回 `403`

### 里程碑 2：AI 问答链路加固

目标：

- 企业知识问答必须在权限过滤后完成检索和回答
- 回答内容和引用来源保持一致
- 低质量命中时明确拒答

改造项：

- 重构 AI 调用链，避免仅靠“Dify 检索后再过滤”
- 优先方案：
  - 按部门、密级或业务域拆分 dataset
  - 或基于 Dify metadata filter 做检索前过滤
- Chat 模式改为“ERP 先检索、ERP 先筛选、ERP 组装上下文、模型基于受控上下文回答”
- 增加问答策略：
  - 无命中拒答
  - 命中分数不足拒答
  - 引用为空时标记回答可信度低
- 为 AI 返回增加字段：
  - `answer_mode`
  - `permission_filtered`
  - `confidence`
  - `retrieved_count`

验收标准：

- 无权限用户无法通过 AI 问答拿到受限文档内容
- 回答中展示的引用与检索片段一致
- 对低相关度问题，系统返回“依据不足”而不是强行生成
- AI 接口日志中可追踪本次问答使用了哪些文档

### 里程碑 3：上传、解析、同步异步化

目标：

- 上传接口只负责接收文件与创建任务
- 文件解析、Dify 同步、状态刷新走后台任务
- 避免大文件阻塞请求线程

改造项：

- 上传流程拆分为：
  - `uploaded`
  - `queued`
  - `parsing`
  - `parsed`
  - `sync_pending`
  - `syncing`
  - `indexed`
  - `failed`
- 引入后台任务机制：
  - 简版可先用 FastAPI `BackgroundTasks`
  - 更稳妥方案用 Celery / RQ / Dramatiq
- 上传接口改为流式保存，避免一次性把大文件全部读入内存
- 每个文档增加任务状态、失败原因、重试次数、最后任务时间
- 增加批量同步和批量刷新状态的任务回执

验收标准：

- 上传 PDF / DOCX / XLSX 时接口能快速返回
- 解析失败时可看到明确错误信息和失败阶段
- 同步 Dify 失败时可以重试，不影响原文档可见性
- 后台任务状态可在前端列表和详情中查看

### 里程碑 4：存储层抽象与数据库迁移准备

目标：

- 从单文件状态存储过渡到可替换的数据层
- 为后续 PostgreSQL 迁移减少业务代码改动

改造项：

- 拆分当前 `ERPStore` 的职责：
  - `DocumentRepository`
  - `PermissionRepository`
  - `AuditLogRepository`
  - `SettingRepository`
  - `DocumentService`
  - `AiService`
- 抽出存储接口，保留现有文件实现作为 `FileRepository`
- 设计 PostgreSQL 表结构：
  - `users`
  - `roles`
  - `departments`
  - `documents`
  - `document_versions`
  - `document_permissions`
  - `audit_logs`
  - `settings`
  - `jobs`
- 为原始文件和解析文本保留文件系统路径字段，后续可平滑迁移到对象存储

验收标准：

- 路由层不再直接依赖大而全的 `ERPStore`
- 文档、权限、审计、设置至少完成逻辑分层
- 数据访问逻辑可以替换成数据库实现而不改 API 契约

### 里程碑 5：测试、日志与运维可观测性

目标：

- 关键链路可回归
- 调试 Dify 与权限问题时有足够的日志证据

改造项：

- 增加自动化测试：
  - 登录与鉴权
  - 文档可见性判断
  - AI 检索权限过滤
  - 上传解析状态流转
  - 设置修改权限
- 增加结构化日志字段：
  - `request_id`
  - `user_id`
  - `action`
  - `document_id`
  - `status`
  - `latency_ms`
- 增加 Dify 调用日志：
  - 接口名
  - dataset_id
  - 耗时
  - 成功或失败
  - 错误摘要
- 审计日志区分：
  - 用户操作
  - 系统任务
  - 外部集成调用

验收标准：

- 关键接口至少有基础测试覆盖
- 任一问答失败都能在日志中定位到失败阶段
- 审计日志能区分“用户手动同步”和“系统自动任务同步”

## 建议的代码层改造顺序

1. 先补鉴权依赖与当前用户上下文
2. 再清理前端对 `user_id` 和 `user` 的显式透传
3. 然后重构 AI 服务，把权限过滤前移
4. 再拆上传解析与 Dify 同步为后台任务
5. 最后拆分 `ERPStore`，为数据库迁移铺路

这样做的原因是：

- 权限问题是当前最高风险
- AI 链路依赖用户身份，必须在鉴权收口后再重构
- 数据层拆分工作量较大，放在前面容易拖慢版本节奏

## 建议的交付清单

### 后端

- 统一认证依赖
- 权限检查装饰器或服务层校验
- AI 服务重构
- 后台任务执行器
- 存储层接口抽象
- 结构化日志
- 自动化测试

### 前端

- 登录态统一管理
- 去除 `user_id` 查询参数依赖
- 文档状态展示补充任务态
- AI 页面增加可信度、过滤状态、引用一致性展示
- 设置页改为按表单整体提交，而不是逐项逐次保存

### 文档

- 更新 `README.md`
- 新增部署配置说明
- 新增 Dify 配置与权限设计说明
- 新增排障手册

## 详细测试步骤

下面的步骤用于验证当前已经落地的 `v0.5.0` 核心改动，重点覆盖：

- Bearer Token 鉴权
- 角色权限控制
- 文档读写与索引权限
- 系统设置访问控制
- AI 接口不再信任前端透传身份
- 前端登录态与受保护页面联调

### 1. 启动系统

执行：

```bash
cd /Users/frankgod/Desktop/RAG_t/Test_python
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
ERP_SECRET_KEY=change-me python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

预期结果：

- 终端出现 `Uvicorn running on http://127.0.0.1:8000`
- 打开 `http://127.0.0.1:8000/health` 返回：

```json
{"status":"ok"}
```

### 2. 验证未登录访问会被拒绝

执行：

```bash
curl -i http://127.0.0.1:8000/erp/v1/documents
```

预期结果：

- 返回 `401 Unauthorized`
- 响应体包含 `Missing bearer token`

### 3. 登录并获取 Bearer Token

管理员登录：

```bash
curl -s http://127.0.0.1:8000/erp/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}'
```

预期结果：

- 返回 `token`
- 返回 `expires_at`
- 返回用户信息中的 `role_id=role-admin`

建议把 token 保存为环境变量：

```bash
export ADMIN_TOKEN="上一步返回的token"
```

再验证当前登录态：

```bash
curl -s http://127.0.0.1:8000/erp/v1/auth/me \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

预期结果：

- 返回新的 `token`
- 返回当前用户信息
- 状态码为 `200`

### 4. 验证管理员可以访问系统设置

执行：

```bash
curl -s http://127.0.0.1:8000/erp/v1/settings \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

预期结果：

- 返回完整 settings 对象
- 能看到 `dify_base_url`、`dify_dataset_id` 等字段

再验证批量保存接口：

```bash
curl -s http://127.0.0.1:8000/erp/v1/settings/batch \
  -X PATCH \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"settings":{"dify_timeout_seconds":60,"dify_send_metadata":false}}'
```

预期结果：

- 返回更新后的 settings
- 状态码为 `200`

### 5. 验证普通用户无法访问系统设置

执行：

```bash
curl -s http://127.0.0.1:8000/erp/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"finance","password":"finance"}'
```

保存 token：

```bash
export FINANCE_TOKEN="finance用户返回的token"
```

访问设置：

```bash
curl -i http://127.0.0.1:8000/erp/v1/settings \
  -H "Authorization: Bearer $FINANCE_TOKEN"
```

预期结果：

- 返回 `403 Forbidden`
- 响应体包含 `Admin access required`

### 6. 验证文档列表与详情依赖服务端当前用户

执行：

```bash
curl -s http://127.0.0.1:8000/erp/v1/documents \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

预期结果：

- 能正常返回文档列表
- 请求中不需要传 `user_id`

如果已有某个文档 ID，例如 `doc-xxxx`，继续执行：

```bash
curl -s http://127.0.0.1:8000/erp/v1/documents/doc-xxxx \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

预期结果：

- 能正常返回文档详情
- 请求中不需要传 `?user_id=...`

### 7. 验证上传接口对非管理员的 owner / department 收口

使用 `ops` 登录：

```bash
curl -s http://127.0.0.1:8000/erp/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"ops","password":"ops"}'
```

保存 token：

```bash
export OPS_TOKEN="ops用户返回的token"
```

上传测试文件：

```bash
printf '采购审批测试文档\n金额超过5万需复核。' > /tmp/erp-test.txt

curl -s http://127.0.0.1:8000/erp/v1/documents/upload \
  -X POST \
  -H "Authorization: Bearer $OPS_TOKEN" \
  -F "file=@/tmp/erp-test.txt" \
  -F "title=采购审批测试" \
  -F "owner_id=u-admin" \
  -F "department_id=dept-finance" \
  -F "visibility=department"
```

预期结果：

- 上传成功
- 返回结果中的 `owner_id` 应该仍然是 `u-ops`
- 返回结果中的 `department_id` 应该仍然是 `dept-ops`
- `parse_status` 应为 `parsed`

这一步的目标是验证：非管理员即使在请求里伪造 `owner_id` 和 `department_id`，服务端也会覆盖为当前登录用户上下文。

### 8. 验证只读用户不能修改文档

先找一个文档 ID，假设为 `doc-xxxx`，然后执行：

```bash
curl -i http://127.0.0.1:8000/erp/v1/documents/doc-xxxx \
  -X PATCH \
  -H "Authorization: Bearer $FINANCE_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"恶意修改"}'
```

预期结果：

- 返回 `403 Forbidden`
- 响应体包含 `Missing permission: document:write`

### 9. 验证编辑用户可以重建索引和同步 Dify

假设上一步上传返回了文档 ID `doc-xxxx`。

本地重建索引：

```bash
curl -s http://127.0.0.1:8000/erp/v1/documents/batch/reindex \
  -X POST \
  -H "Authorization: Bearer $OPS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"ids":["doc-xxxx"]}'
```

预期结果：

- 返回 `affected=1`
- 返回 `jobs`
- 文档状态后续可在详情页看到 `index_status=completed` 或相应状态

如果本地已经配置 Dify，再测试同步：

```bash
curl -s http://127.0.0.1:8000/erp/v1/documents/doc-xxxx/sync-to-dify \
  -X POST \
  -H "Authorization: Bearer $OPS_TOKEN"
```

预期结果：

- 若 Dify 配置正确，返回同步结果和 `dify_document_id`
- 若 Dify 未配置，返回 `400`
- 若 Dify 服务异常，返回 `502`

### 10. 验证 AI 接口不再信任请求体 user 字段

当前代码已经移除了 AI 请求体中的 `user` 透传，所以直接测试：

```bash
curl -s http://127.0.0.1:8000/erp/v1/ai/retrieve \
  -X POST \
  -H "Authorization: Bearer $OPS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"采购审批超过5万怎么处理？","top_k":5}'
```

预期结果：

- 请求体中不需要再传 `user`
- 服务端自动按当前 Bearer Token 用户过滤可见文档
- 返回 `query` 与 `chunks`

继续测试 AI 问答：

```bash
curl -s http://127.0.0.1:8000/erp/v1/ai/chat \
  -X POST \
  -H "Authorization: Bearer $OPS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"question":"采购审批超过5万怎么处理？","top_k":5}'
```

预期结果：

- 若 Dify App 已配置，返回：
  - `answer`
  - `conversation_id`
  - `answer_mode`
  - `permission_filtered`
  - `confidence`
  - `retrieved_count`
  - `citations`
- 若无可见文档命中，返回“没有在你当前可见的知识库范围内检索到相关内容”

### 11. 验证前端联调

浏览器打开：

```text
http://127.0.0.1:8000/app
```

测试步骤：

1. 使用 `admin` 登录，确认可以进入系统。
2. 进入“系统设置”，确认页面可正常打开并保存。
3. 退出后使用 `finance` 登录。
4. 确认左侧导航不再显示“权限配置 / 审计日志 / 系统设置”。
5. 进入“文档库”和“AI 问答”，确认页面可正常使用。
6. 刷新浏览器，确认前端会通过 `/erp/v1/auth/me` 校验并恢复登录态。

预期结果：

- 登录后所有请求都会自动带 Bearer Token
- 普通用户不会在前端看到管理员专属页面入口
- token 失效或缺失时，页面会回到登录页

### 12. 验证错误场景

测试无效 token：

```bash
curl -i http://127.0.0.1:8000/erp/v1/documents \
  -H "Authorization: Bearer invalid-token"
```

预期结果：

- 返回 `401`
- 响应体包含 `Invalid or expired token`

测试不支持的 settings key：

```bash
curl -i http://127.0.0.1:8000/erp/v1/settings \
  -X PATCH \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"key":"unexpected_key","value":"test"}'
```

预期结果：

- 返回 `400 Bad Request`
- 响应体包含 `Unsupported setting`

## 当前版本建议的最小回归清单

每次修改后，至少重复以下 8 项：

1. 未登录访问 `/erp/v1/documents` 返回 `401`
2. `admin` 登录成功并可访问 `/erp/v1/settings`
3. `finance` 访问 `/erp/v1/settings` 返回 `403`
4. `ops` 上传文档成功，`owner_id` 不会被伪造
5. `finance` 修改文档返回 `403`
6. `ops` 调用 `/erp/v1/ai/retrieve` 不需要传 `user`
7. 前端刷新后仍能自动恢复登录态
8. 无效 token 会触发重新登录

## 风险与注意事项

- 如果继续使用单一 Dify dataset，权限边界仍然会偏脆弱
- 如果没有后台任务，文件解析和同步会持续拖慢接口响应
- 如果没有 token 校验，前端界面再完整也不能算企业可用
- 如果数据层不拆分，后续接 PostgreSQL 时会出现大面积重构

## 建议的版本定义

`v0.5.0` 建议定义为：

“完成 ERP 鉴权收口、AI 权限链路加固、任务化上传同步、存储层解耦与基础可观测性建设的稳定化版本。”

## 建议的下一版本预告

在 `v0.5.0` 完成后，`v0.6.0` 可以继续推进：

- PostgreSQL 正式落地
- 对象存储接入
- OCR 接入
- 文档审核流
- 更细粒度的下载权限和水印策略
- 多知识库路由和更完整的企业智能体编排
